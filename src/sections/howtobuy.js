export function initHowToBuy() {
  /* Big background step number scales up as that step becomes "active" */
  /* The scroll scrub animation for the background numbers is handled
     via CSS — each step's bg-num transitions on the step entering view */
}
